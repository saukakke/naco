@extends('layouts.portal') @section('content')<div class="page-header"><div><p class="eyebrow">{{ $period->year }} — Period {{ $period->period }}</p><h1>Ward Four-Monthly Report</h1><p>{{ $ward->name }} · {{ $period->starts_on->format('d M Y') }} – {{ $period->ends_on->format('d M Y') }}</p></div></div><div class="form-card"><form method="POST" action="{{ route('portal.reports.store') }}">@csrf<label>Summary<textarea name="summary" required>{{ old('summary') }}</textarea></label><label>Activities<textarea name="activities_text" placeholder="Describe major activities"></textarea></label><label>Membership<textarea name="membership_text" placeholder="Membership statistics"></textarea></label><label>Training<textarea name="training_text" placeholder="Training and courses conducted"></textarea></label><label>Self-reliance<textarea name="self_reliance_text" placeholder="Self-reliance activities"></textarea></label><label>Finance<textarea name="finance_text" placeholder="Financial summary"></textarea></label><button class="btn btn-primary" type="submit">Save Draft</button></form></div>@endsection
