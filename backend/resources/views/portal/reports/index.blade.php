@extends('layouts.portal') @section('content')<div class="page-header"><div><p class="eyebrow">Reporting</p><h1>Four-Monthly Reports</h1><p>Ward reports move through LGA, State and National review.</p></div>@if(auth()->user()->isHcs())<a class="btn btn-primary" href="{{ route('portal.reports.create') }}">Create Report</a>@endif</div><div class="table-card"><div class="table-wrap"><table><thead><tr><th>Ward</th><th>Period</th><th>Status</th><th>Submitted</th><th></th></tr></thead><tbody>@forelse($reports as $report)<tr><td>{{ $report->ward->name }}</td><td>{{ $report->period->year }} — Period {{ $report->period->period }}</td><td>{{ str_replace('_',' ',ucfirst($report->status)) }}</td><td>{{ $report->submitted_at?->format('d M Y H:i') ?? 'Draft' }}</td><td><a class="btn btn-sm" href="{{ route('portal.reports.show',$report) }}">View</a></td></tr>@empty<tr><td colspan="5">No reports found.</td></tr>@endforelse</tbody></table></div></div>{{ $reports->links() }} @endsection
