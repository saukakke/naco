@extends('layouts.portal') @section('content')<div class="page-header"><div><p class="eyebrow">Super Administration</p><h1>Role Management</h1><p>Grant or revoke administrative access for cadets.</p></div></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="table-card"><div class="table-wrap"><table><thead><tr><th>User</th><th>Email</th><th>Current role</th><th>Change role</th><th>Action</th></tr></thead><tbody>@foreach($users as $user)<tr><td>{{ $user->name }}</td><td>{{ $user->email }}</td><td>{{ $user->role }}</td><td><form method="POST" action="{{ route('portal.admin.roles.update',$user) }}">@csrf @method('PATCH')<select name="role"><option value="cadet" @selected($user->role==='cadet')>Cadet</option><option value="instructor" @selected($user->role==='instructor')>Instructor</option><option value="unit_commander" @selected($user->role==='unit_commander')>Unit Commander</option><option value="admin" @selected($user->role==='admin')>Admin</option></select><button class="btn btn-sm btn-primary">Save</button></form></td><td>@if($user->role==='admin')<form method="POST" action="{{ route('portal.admin.roles.revoke',$user) }}">@csrf @method('PATCH')<button class="btn btn-sm">Revoke Admin</button></form>@endif</td></tr>@endforeach</tbody></table></div></div>{{ $users->links() }}@endsection
