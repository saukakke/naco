<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>{{ $title ?? 'NACO Portal' }}</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="portal-shell"><aside class="portal-sidebar"><div><a class="brand brand-light" href="{{ route('portal.dashboard') }}"><span class="brand-mark">N</span><span>NACO</span></a><p class="sidebar-caption">Personnel Command Portal</p></div><nav aria-label="Portal navigation"><span class="nav-label">Workspace</span><a class="nav-item" href="{{ route('portal.dashboard') }}"><span>⌂</span> Dashboard</a><a class="nav-item" href="{{ route('portal.personnel.show', auth()->user()->cadet ?? '') }}"><span>◉</span> My Profile</a><span class="nav-label">Operations</span><a class="nav-item" href="{{ route('portal.instructors.index') }}"><span>✦</span> Instructors</a><a class="nav-item" href="{{ route('portal.reports.index') }}"><span>▤</span> Reports</a><a class="nav-item" href="{{ route('portal.unit-transfers.index') }}"><span>⇄</span> Unit Transfers</a><a class="nav-item" href="{{ route('portal.ward-transfers.index') }}"><span>↗</span> Ward Transfers</a><a class="nav-item" href="{{ route('portal.id-card-renewals.index') }}"><span>▣</span> ID Cards</a><span class="nav-label">System</span><a class="nav-item" href="{{ route('portal.notifications.index') }}"><span>◌</span> Notifications</a>@if(auth()->user()->role==='super_admin')<a class="nav-item" href="{{ route('portal.admin.roles') }}"><span>◆</span> Role Management</a>@endif</nav><form method="POST" action="{{ route('portal.logout') }}">@csrf<button class="sign-out" type="submit"><span>↪</span> Sign out</button></form></aside><div class="portal-main"><header class="portal-topbar"><div><p class="eyebrow">NACO Personnel Portal</p><h1>{{ $pageTitle ?? 'Command Centre' }}</h1></div><div class="user-chip"><span class="avatar">{{ strtoupper(substr(auth()->user()->name ?? 'N',0,1)) }}</span><span><strong>{{ auth()->user()->name ?? 'Personnel' }}</strong><small>{{ str_replace('_',' ',ucwords(auth()->user()->role ?? 'user')) }}</small></span></div></header><main class="portal-content">@yield('content')</main></div></body></html>
